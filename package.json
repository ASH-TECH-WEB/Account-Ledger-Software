{
  "name": "account-ledger-backend",
  "version": "1.0.0",
  "description": "Backend API for Account Ledger Software",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "vercel-build": "npm install",
    "keep-alive": "node keep-alive.js",
    "start-with-keepalive": "node server.js & node keep-alive.js",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "cleanup": "node scripts/cleanup-console.js",
    "build:prod": "npm run cleanup",
    "migrate": "node scripts/migrate-to-supabase.js",
    "setup-db": "node scripts/setup-database.js",
    "check-db": "node scripts/check-database.js",
    "clear-db": "node scripts/clear-database.js",
    "setup-supabase": "echo 'Please run the SQL schema in your Supabase SQL editor'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "account",
    "ledger",
    "api",
    "nodejs",
    "express"
  ],
  "author": "Account Ledger Team",
  "license": "MIT",
  "dependencies": {
    "@supabase/supabase-js": "^2.54.0",
    "axios": "^1.11.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.4",
    "connect-timeout": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "eslint": "^9.32.0",
    "globals": "^16.3.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
